[project]
name = "selective-learning-finetune"
version = "0.1.0"
description = "Config-driven fine-tuning runner for selective learning experiments"
requires-python = ">=3.11"
dependencies = [
    "openweights",
    "pydantic",
    "python-dotenv",
    "pyyaml",
]

[tool.uv]
package = false
